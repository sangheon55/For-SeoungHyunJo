# 후지와라 LLM 돌발 난입 기획

## 핵심 아이디어

학습 타이머 종료 후 일정 확률로 후지와라가 난입하고, LLM이 현재 학습 상황에 맞는 짧은 학생회 대화를 생성한다.

- 난입 여부와 횟수는 앱 코드가 결정한다.
- LLM은 캐릭터, 표정과 대사만 생성한다.
- 생성 실패 시 미리 작성한 기본 대사를 사용한다.

## 기본 발생 조건

- 한 세션이 25분 이상일 때만 후보
- 기본 발생 확률 15%
- 2시간 이상 세션은 당일 첫 난입 확정
- 발생한 난입 중 3%는 후지와라가 먼저 종료를 선언하는 희귀 역난입
- 하루 최대 2회
- 동일한 이벤트와 대사는 연속 사용하지 않음
- 설정에서 `후지와라 돌발 난입`을 끌 수 있음

## 연출 순서

1. 학습 타이머 종료
2. 카구야가 기존 종료 대사 표시
3. 약 3초 후 후지와라 난입
4. 후지와라 대사를 약 3~4초 표시
5. 카구야가 후속 반응
6. 기존 종료 상태로 복귀

종료 직전에는 후지와라가 왼쪽으로 미끄러져 퇴장한다.

## 상황별 난입

- 25분 이상: 간식과 짧은 휴식
- 1시간 이상: 게임 한 판 제안
- 2시간 이상: 놀람과 강제 휴식 주장
- 오후 10시 이후: 야식과 휴식
- 연속 학습 7일 이상: 축하 파티 주장

희귀 역난입은 전체 세션의 3%가 아니라 이미 난입이 발생한 경우 중 3%다.
일반 세션 기준 실제 확률은 `15% × 3% = 0.45%`다.

역난입이 시작될 때는 후지와라 전용 16:9 특수 컷인을 약 1.1초 표시한 뒤
양쪽 캐릭터 대화로 전환한다.

## 로컬 테스트 환경

개발 서버의 학습 타이머 화면에는 프로덕션에서 제거되는 테스트 패널을 표시한다.

- 상황 선택: 25분, 1시간, 2시간, 야간, 7일 연속
- LLM 대사 사용 여부 선택
- 일반 난입 강제 재생
- 역난입과 특수 컷인 강제 재생
- 학습 기록 및 일일 난입 횟수에는 반영하지 않음

예시:

> 카구야 `smug`  
> 제법이네요.

> 후지와라 `smile`  
> 두 시간이나 했으면 이제 놀아도 되는 거죠?

> 카구야 `neutral`  
> 그 보상은 누가 정한 거죠?

## LLM 입력

사용자의 학습 원문 전체가 아니라 이벤트 생성에 필요한 최소 정보만 전달한다.

```json
{
  "event": "fujiwara_interrupt",
  "subject": "한국사",
  "sessionMinutes": 126,
  "todayMinutes": 284,
  "streakDays": 7,
  "timeOfDay": "night",
  "previousLines": [
    "두 시간이나 했으면 이제 놀아도 되는 거죠?"
  ]
}
```

## LLM 출력

LLM은 UI에서 바로 검증하고 재생할 수 있는 JSON만 반환한다.

```json
{
  "beats": [
    {
      "character": "fujiwara",
      "face": "smile",
      "text": "두 시간이나 했으면 이제 놀아도 되는 거죠?",
      "inner": "(보드게임 한 판 정도는 괜찮겠지?)",
      "delayMs": 3000
    },
    {
      "character": "kaguya",
      "face": "neutral",
      "text": "그 보상은 누가 정한 거죠?",
      "inner": "",
      "delayMs": 6500
    }
  ]
}
```

## 허용 값

```js
character: 'kaguya' | 'fujiwara'

face:
  'neutral' |
  'blink' |
  'smile' |
  'smug' |
  'flustered'
```

## 생성 규칙

- 전체 장면은 최대 3개
- 대사 한 줄은 최대 60자
- 후지와라는 밝고 엉뚱하며 게임을 좋아하는 말투 사용
- 카구야는 절제되고 품위 있는 반응 사용
- 캐릭터를 지나치게 과장하거나 악의적으로 표현하지 않음
- 학습 결과를 조롱하거나 죄책감을 주지 않음
- 직전 대사와 유사한 문장 반복 금지
- 허용되지 않은 캐릭터와 표정은 반환 금지

## 기본 대사

LLM 연결 실패나 잘못된 응답에 사용한다.

### 25분 이상

```json
{
  "character": "fujiwara",
  "face": "smile",
  "text": "수고했어요! 이제 간식 시간인가요?"
}
```

### 1시간 이상

```json
{
  "character": "fujiwara",
  "face": "smug",
  "text": "저라면 중간에 게임 한 판 했을 텐데요."
}
```

### 2시간 이상

```json
{
  "character": "fujiwara",
  "face": "flustered",
  "text": "잠깐만요, 정말 두 시간 내내 공부한 거예요?"
}
```

## 로컬 발생 기록

난입 횟수와 중복 방지를 위해 `localStorage`에 당일 기록을 저장한다.

```json
{
  "date": "2026-07-27",
  "count": 1,
  "lastEventId": "fujiwara-interrupt-play"
}
```

Firebase 동기화는 1차 구현 범위에서 제외한다.

## 기획 중

### 하루 종료 학생회 평가

하루 학습 상태에 따라 카구야, 하야사카, 후지와라 또는 이시가미가 평가한다.

## 구현 순서

- [ ] 난입 확률과 하루 제한 로직 구현
- [ ] 후지와라와 카구야를 전환할 수 있는 공통 레이어 구현
- [ ] 기존 AI 서버에 `fujiwara_interrupt` 이벤트 추가
- [ ] LLM JSON 응답 검증
- [ ] 장면 순차 재생 기능 구현
- [ ] 기본 대사 fallback 추가
- [ ] `후지와라 돌발 난입` 설정 추가
- [ ] 중복 방지용 로컬 기록 추가
- [ ] 경계값과 실패 상황 테스트
