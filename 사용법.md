# 조성현 합격 플래너 — 사용 안내 (v1.3.1)

## 🚀 앱 실행하기

빌드된 앱은 아래 폴더 안에 있습니다.

```
release\Seonghyeon Planner-win32-x64\
   └─ Seonghyeon Planner.exe   ← 이 파일을 더블클릭하면 실행
```

> 이 **폴더 전체가 앱**입니다(약 270MB). `.exe` 하나만 떼어내면 실행되지 않으니,
> 폴더째로 다루세요.

## 👤 친구(조성현)에게 전달하는 법 (처음 설치)

1. `Seonghyeon Planner-win32-x64` 폴더를 **압축(zip)** 합니다.
2. 압축 파일을 친구에게 전달합니다.
3. 친구는 압축을 풀고 안의 `Seonghyeon Planner.exe`를 더블클릭하면 됩니다.
   (설치 과정 없음 · 인터넷 불필요)

## 🔁 업데이트 — **앱 안에서 클릭 한 번** (v1.3.1+)

### 친구가 받아서 해야 할 일 (가장 쉬운 방법)

1. 앱 실행 → **사이드바 [⚙️ 설정]** → 아래로 스크롤
2. **🔄 앱 업데이트** 카드의 **[🔍 업데이트 확인]** 클릭
3. 새 버전이 있으면 변경 내용이 나옴 → **[📥 다운로드 후 자동 설치]** 클릭
4. 잠시 기다린 후 **[⚡ 지금 적용]** → 앱이 자동 종료되고 새 버전으로 다시 켜짐
5. 끝! 기존 기록(오답·메모·타이머·할 일·통계 전부)은 그대로 유지됩니다.

> 인터넷 연결만 있으면 GitHub에서 자동으로 받습니다. 권한 오류로 실패하면
> 같은 카드의 **[🌐 GitHub Releases 페이지 열기]** 버튼으로 zip을 직접 받아 수동 교체할 수 있습니다.

### 📦 수동 업데이트 (zip 직접 받았을 때 또는 인터넷 안 될 때)

1. 현재 실행 중인 앱이 있으면 **완전히 종료**합니다.
2. 새로 받은 zip을 풉니다.
3. 기존 `Seonghyeon Planner-win32-x64` 폴더에 **덮어쓰기** (또는 통째로 교체).
4. 새 폴더 안의 `Seonghyeon Planner.exe` 더블클릭 → 끝.

> ✅ **기존 데이터는 모두 그대로 유지됩니다.**
> 기록은 앱 폴더가 아니라 `C:\Users\<사용자>\AppData\Roaming\Seonghyeon Planner\planner-data.json`에
> 저장되기 때문에, 앱 폴더를 통째로 교체해도 사라지지 않습니다.

## 💾 데이터는 어디에 저장되나

- 모든 기록은 앱 폴더가 아니라 **고정된 별도 파일**에 저장됩니다:
  ```
  C:\Users\<사용자>\AppData\Roaming\Seonghyeon Planner\planner-data.json
  ```
- 앱 폴더(.exe)를 **새 버전으로 통째로 교체해도** 이 데이터 파일은 그대로 남습니다.
- 새 버전에서 기능이 추가돼도 기존 데이터에 자동으로 맞춰집니다.
  (예: v1.3에서 오답노트가 추가되었지만 v1.2 사용자는 빈 오답 목록으로 자동 초기화됨)
- 앱 안 [설정] → 데이터 폴더 열기 / 내보내기 / 가져오기 로 직접 관리할 수 있습니다.
  (다른 PC로 옮길 때: 내보내기 → 파일 전달 → 새 PC에서 가져오기)

## 🔁 매주 업데이트하는 방법 (개발자용)

```powershell
# 1) package.json의 "version" 값을 올림
# 2) 빌드
npm run dist

# 3) zip 압축
Compress-Archive -Path "release\Seonghyeon Planner-win32-x64" `
                 -DestinationPath "release\조성현 합격 플래너 v1.x.zip" -Force

# 4) GitHub Release 생성 + 업로드
gh release create v1.x.0 "release\조성현 합격 플래너 v1.x.zip" `
   --title "v1.x — 한 줄 요약" --notes "변경 사항"
```

→ 친구는 별다른 안내 없이도 앱에서 [업데이트 확인] 누르면 자동으로 받아 갑니다.
zip을 직접 전달할 필요가 없어졌습니다.

> ⚠️ 태그 이름은 반드시 `v` 접두사(`v1.4.0`), asset은 `.zip` 확장자여야 합니다.

개발 중 빠르게 확인하려면:
```
npm run dev      # 브라우저에서 미리보기
npm run app      # 데스크톱 앱 창으로 실행
```

## 🧩 현재 구현된 기능 (v1.3.1)

| 화면 | 기능 |
|---|---|
| 홈 | 날짜, 매일 다른 응원 메시지, 나무 위젯, D-day, 오늘 요약·할 일, **오늘 복습 알림 칩** |
| 플래너 | 날짜별 할 일 추가/체크/삭제 (과목 태그), 학습 타임라인, 하루 메모 |
| 학습 타이머 | ① 일일 공부량 타이머(과목별 기록·목표 달성률·누적) ② 일반 스톱워치(랩) |
| 과목 메모 | 과목별 메모 작성/수정/삭제 |
| 회독 관리 | 교재별 회독 진행도 추적 |
| **오답노트** | **NEW** — 과목별 오답 등록(문제·정답·해설·이미지·태그·풀이 시간·유형) |
| | **에빙하우스 자동 복습 1·3·7·14·30일** + **퀴즈 모드**(정답 가림 → 맞춤/틀림 평가) |
| | 틀리면 1단계로 리셋, 5단계 모두 통과하면 "완료" |
| 통계 | 전일·전주·전달 비교, 학습 잔디, 과목별 시간, 연속 학습일, **과목별 약점 카드** |
| 나의 숲 | 나무 성장 단계, 완성한 큰나무 모음 |
| 설정 | 과목 추가/삭제, 시험일, 일일·주간 목표 공부 시간, 데이터 관리, **🔄 앱 업데이트(v1.3.1+)** |

### ❌ 오답노트 사용 흐름

1. **사이드바 → 오답노트** 클릭
2. **[+ 새 오답]** → 과목 / 문제 / 내 답 / 정답 / 해설 / 난이도·유형 / 출처·태그 / 이미지 입력 후 저장
3. 등록 즉시 **1·3·7·14·30일 후** 5개 복습일이 자동 예약됨
4. 매일 홈 상단의 **"오늘 복습 N개"** 칩 클릭 또는 오답노트의 **▶ 오늘 복습 풀기**
5. **퀴즈 모드** 시작:
   - 문제만 표시 → 풀이 시간 측정 → **[정답 보기]** → 정답·해설 노출
   - **[✓ 맞춤]** : 다음 단계로 진행 (●●○○○ → ●●●○○ …)
   - **[✗ 틀림]** : 1단계로 리셋, 오늘부터 다시 1·3·7·14·30일
6. 5단계 모두 통과 시 **"완료 🎉"** 뱃지가 붙고 활성 목록에서 빠짐
7. **통계 → "🔍 과목별 약점"** 카드에서 오답 수·미완료 수·정답률을 한눈에 확인

### 나무 성장 단계 (8단계 · 누적 1,800시간에 큰나무 완성)
씨앗 → 새싹(25h) → 떡잎(75h) → 묘목(175h) → 어린나무(375h)
→ 청년나무(720h) → 우거진나무(1,200h) → 큰나무(1,800h)

## ⚠️ 참고

- 앱은 인터넷 없이 동작하며 데이터는 PC 안에만 저장됩니다.
- Windows에서 처음 실행 시 "Windows의 PC 보호" 경고가 뜰 수 있습니다
  (서명되지 않은 앱이라서) → **추가 정보 → 실행**을 누르면 됩니다.
- 오답노트 이미지는 자동으로 가로 1200px / JPEG 70%로 압축됩니다.
  그래도 사진을 너무 많이 첨부하면 데이터 파일이 커질 수 있으니 핵심 스크린샷 위주로.
